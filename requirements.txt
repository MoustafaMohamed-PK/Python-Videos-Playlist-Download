# Core download engine
yt-dlp>=2024.8.6

# Web UI (optional -- only needed to run webmain.py, not the CLI)
Flask>=3.0
waitress>=3.0

# Note: FFmpeg is a required *system* dependency (not a Python package).
# It must be installed separately and available on PATH.
# See README.md for platform-specific installation instructions.
